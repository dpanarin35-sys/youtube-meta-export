{
  "name": "youtube-meta-exporter",
  "version": "1.0.0",
  "private": true,
  "description": "Export metadata from every video on the signed-in YouTube channel.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
